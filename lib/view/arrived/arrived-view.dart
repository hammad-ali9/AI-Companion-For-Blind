import 'package:flutter/material.dart';

class ArrivedView extends StatelessWidget {
  const ArrivedView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
