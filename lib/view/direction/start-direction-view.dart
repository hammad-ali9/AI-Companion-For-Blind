import 'package:flutter/material.dart';

class StartDirectionView extends StatelessWidget {
  const StartDirectionView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
